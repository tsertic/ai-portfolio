<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" fill="none">
  <!-- Outer ring with gap (spinner) -->
  <circle cx="100" cy="100" r="70" stroke="currentColor" stroke-width="6" opacity="0.12"/>
  <path d="M100 30 A70 70 0 1 1 40 155" stroke="currentColor" stroke-width="6" stroke-linecap="round" opacity="0.55"/>
  <!-- Middle ring -->
  <circle cx="100" cy="100" r="50" stroke="currentColor" stroke-width="4" opacity="0.1"/>
  <path d="M100 50 A50 50 0 0 1 150 100" stroke="currentColor" stroke-width="4" stroke-linecap="round" opacity="0.35"/>
  <!-- Inner ring -->
  <circle cx="100" cy="100" r="30" stroke="currentColor" stroke-width="3" opacity="0.08"/>
  <path d="M100 70 A30 30 0 0 0 70 100" stroke="currentColor" stroke-width="3" stroke-linecap="round" opacity="0.3"/>
  <!-- Center dot -->
  <circle cx="100" cy="100" r="6" fill="currentColor" opacity="0.4"/>
  <!-- Percentage text -->
  <text x="100" y="106" font-family="monospace" font-size="11" fill="currentColor" opacity="0.4" text-anchor="middle">...</text>
</svg>
